#include <stdio.h>
#include <stdlib.h>

#define TAM 10
/*int main()
{
    //declarar o vetor com 10 posicoes
    int numeros[TAM];
    int i, maior = 0;

    //inserir os 10 valores
    for(i = 0; i < TAM; i++){
        printf("Informe o valor n%d: ", i + 1);
        scanf("%d", &numeros[i]);
    }

    //encontrar o maior valor
    for (i = 0; i < TAM; i++){
        if(numeros[i] > maior){
            maior = numeros[i];
        }
    }
    printf("O maior numero eh: %d", maior);
    return 0;
}*/
